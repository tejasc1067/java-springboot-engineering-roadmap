# repository-demo

A `BookRepository extends JpaRepository<Book, Long>` with no custom methods. Exercises the standard CRUD + pagination + sort surface that JpaRepository inherits.

Run:

```
mvn test
```
