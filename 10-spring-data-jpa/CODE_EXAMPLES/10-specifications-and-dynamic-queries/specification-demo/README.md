# specification-demo

`JpaSpecificationExecutor<T>` and `Specification<T>` — dynamic queries built from optional filters.

Run:

```
mvn test
```

Watch the SQL change per test as different filters are passed in.
