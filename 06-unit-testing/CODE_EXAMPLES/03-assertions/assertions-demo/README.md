# assertions-demo

The same checks written two ways: JUnit's built-in assertions and AssertJ's fluent ones.

Run the tests:

```
mvn test
```

Read both test files side by side. Then make one assertion fail in each (change an expected value) and compare the failure messages — that contrast is the point of the topic.
